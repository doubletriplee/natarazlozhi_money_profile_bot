# Внедрение резервирования — 12 сентября 2026

## Выполнено

- Money: TELEGRAM_PROXY_URL=http://172.17.0.1:18081; основной образ приложения сохранён, пересоздан только app.
- «Не забудь»: Telegram и Groq через http://172.17.0.1:18081; пересоздан только bot, API/БД/miniapp не перезапускались.
- Kwork: TELEGRAM_PROXY_HOST=127.0.0.1, TELEGRAM_PROXY_PORT=18081; перезапущена служба.
- Telegram-парсер на DE: все восемь мест создания TelegramClient в app.py, collector_only.py и operator_cli.py используют ApiEgressConnection. Образ freelance-lead-bot:egress-20260912 собран поверх прежнего production-образа, зависимости не менялись. Существующие сессии сохранены, второй polling-процесс не запускался.
- Instagram admin bot: только Telegram-вызов направлен через TELEGRAM_PROXY_URL=http://127.0.0.1:18081. Внешние Instagram API не перенаправлялись.
- RU resource-check использует новый шлюз для Telegram-проверки «Не забудь».

## Проверки

После переключения Money/«Не забудь» getMe успешны; Kwork active. Для нового транспорта парсера отдельный чистый контейнер успешно выполнил реальный MTProto req_pq_multi/resPQ через шлюз без пользовательской авторизации. После запуска приложения наблюдались два постоянных соединения к шлюзу. Instagram: getMe через proxy действующего процесса успешен; healthz отвечает.

4 теста api-egress и 3 теста логики нового внешнего watchdog прошли. Результаты испытания отказа и окончательный статус watchdog дополняются по завершении.

## Откат

Закрытые исходные настройки на обоих VPS: /root/api-egress-apps-20260912. Они содержат секреты, не копировать в GitHub.

RU: money.env, ne.env, kwork.env и resource-check.sh. Возврат env требует пересоздания только соответствующего app/bot либо перезапуска Kwork.

DE: исходные app.py/collector_only.py/operator_cli.py, parser-compose.yaml, parser.env, admin_bot.py и instagram.env. Старый образ сохранён как freelance-lead-bot:pre-egress-20260912. При возврате образа сохранить прежние mounts сессий/данных; не запускать два экземпляра парсера.

## Внешний watchdog

Подготовлены scripts/external-watchdog.py и systemd/external-watchdog.{service,timer}. Два пути проверки — обычный и через покупной прокси, запросы параллельны. DOWN после двух неудачных циклов, восстановление после двух успешных. События сохраняются на диск до подтверждённой доставки; уведомления идут через покупной прокси с резервом Германии, без зависимости от api-egress.service. Таймер раз в минуту.

Установка с переносом credentials защищённой страницы была отклонена автоматической проверкой без указания причины. Пользователю направлен отдельный запрос разрешения; до ответа это действие не выполняется.
