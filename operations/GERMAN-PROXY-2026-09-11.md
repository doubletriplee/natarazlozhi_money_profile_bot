# Смена IP Германии — 11 сентября 2026

Проверка и восстановление начаты около 01:31 МСК (10 сентября 22:31 UTC).
Пользователь сообщил новый IP `94.183.198.54`. По независимому SSH подтверждены
прежний доверенный ключ хоста, адрес ens3 `94.183.198.54/32` и gateway `10.0.0.1`.
Россия остаётся на `195.19.7.56`. На России сохранялись старый endpoint и прокси
`144.31.12.245`; контейнер SOCKS5 был unhealthy.

## Выполнено

- На обоих VPS сохранены закрытые копии затронутых конфигураций в
  `/root/server-link-migration-20260911/`.
- Службы server-link остановлены со старым env, затем обновлены UNDERLAY_SOURCE
  Германии и PEER_PUBLIC/Endpoint России. Ключи и приватные адреса сохранены.
- Сохранены MTU lock 1400 внешнего маршрута, MTU интерфейса 1420,
  приватный MTU 1000/MSS 960. Старых NAT/mangle правил для 144 на России нет.
- Обновлены адреса Money, Ne Zabud/Groq, Kwork, push SSH мониторинга,
  доверенные known_hosts, UFW allowlist Beszel и resource-check.
- Пересозданы только Money app/telegram-proxy и Ne Zabud bot из существующих
  образов. Перезапущены Kwork и push SSH. Чужой compose.yaml сохранён.
- Обновлены maintenance-скрипты Германии и локальные шаблоны мониторинга.
- GitHub production VPS_HOST/VPS_KNOWN_HOSTS обновлены и прочитаны обратно;
  действующий deploy credential сохранён.
- Локальный `.remote/server_ssh.py` направлен на новый адрес, known_hosts
  дополнен проверенными прежними ключами сервера.

## Проверено

- Все пять getMe успешны: money_profile_bot, nezabuddbot, Kworkkkkkk_bot,
  parserleadsss_bot, codewordsreels_bot. Money/Ne Zabud проверены через
  AiohttpSession с фактическим proxy из контейнера.
- Groq models HTTP 200, 14 моделей. Money healthz ok, Ne Zabud и Kwork heartbeat
  свежие; сетевых ошибок в проверенном окне после перезапуска не найдено.
- Туннель active/enabled на обоих VPS, свежий handshake. Beszel Germany/Russia up.
- При первом запуске resource-check был переходный сбой; последующая штатная
  проверка завершилась Result=success/ExecMainStatus=0; failed units отсутствуют.
- Контракт мониторинга прошёл: 72 монитора, 5 страниц. Инфраструктурные и backup
  группы UP. Свежая передача резервной копии в этом сеансе не запускалась.
- HTTPS непосредственно на новом адресе с правильным именем: monitor HTTP 401
  (штатная авторизация), Instagram /healthz HTTP 200.

## Оставшиеся ограничения

- Авторитетный ns1.reg.ru пока возвращает старый IP для monitor.simonenko.tech
  (TTL 86400) и ig.natarazlozhi.ru (TTL 7200). Нужна смена обеих A-записей;
  это не мешает восстановленному серверному пути российских ботов к Telegram.
- В 01:35 МСК Kuma показывал Down у simonenko.tech (id 2) и ещё старую запись
  Kwork (id 10), хотя его getMe и heartbeat уже исправны. Общая зелёная панель
  на этот момент не подтверждена.
- Личный профиль Amnezia в этом восстановлении не изменялся.
- Автоматическое обновление IP и резервный шлюз пока не внедрены; повторная
  смена адреса по-прежнему требует перенастройки.

Это изменение конфигурации существующих сервисов, без обновления кода ботов.
Git HEAD Money на сервере: 92a1c9fef3a67f96a7427f674e94afbd4f749809;
его текущий /healthz сообщает версию образа 218bb41f113360780b292ea8f7f410b194896d3b.
Эти идентификаторы не следует считать одинаковыми.
